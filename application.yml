server:
  port: 8080
spring:
  application:
    name: greeting-service
management:
  endpoints:
    web:
      exposure:
        include:
          - "*"
    endpoint:
      health:
        show-details: true
greeting-service:
  greeting: "Hello"
  default-value: "World"